/**
 * Git => VCS
 *
 * git branch -m master main
 * master => main 이동
 *
 * git config --global init.defaultbranch main
 * => 초기화 할 때 마스터 말고 main으로 지정
 *
 * git branch login => branch 생성
 * git branch => branch 종류 확인 가능
 * branch 이동할 때는 checkout말고 switch쓰는게 좋음
 * branch 지울 때 git branch -D login
 *
 * 만들면서 branch 이동할 때 git switch -c login
 */
